services:
  - type: web
    name: facebook-autopilot
    env: docker
    plan: free
    region: oregon
    dockerfilePath: Dockerfile
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
      - key: OWNER_USERNAME
        value: admin
